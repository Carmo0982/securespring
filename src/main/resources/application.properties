# In resources/application.properties
# The format used for the keystore. It could be set to JKS in case it is a JKS file
server.ssl.key-store-type=PKCS12
# The path to the keystore containing the certificate
server.ssl.key-store=classpath:keystores/ecikeystore.p12
# The password used to generate the certificate
server.ssl.key-store-password=123456
# The alias mapped to the certificate
server.ssl.key-alias=ecikeypair
server.ssl.enabled=true

# Basic login credentials (hashed with BCrypt)
app.security.user.name=tdseuser
app.security.user.password-hash=$2a$10$7EqJtq98hPqEX7fNZaFWoOHi7qN8hA6fQtbDNRsTA70vsK1tnE.bG

# Apache HTTPS endpoint used by Spring to validate TLS-protected REST/download access
app.apache.download-url=https://localhost/downloads/hello.txt